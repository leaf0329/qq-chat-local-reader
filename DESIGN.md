# qq-chat-local-reader 设计基线

> 状态：产品需求已确认，等待实施前技术调研与最终确认。本文只记录已经确认的决定；技术决策必须基于文档、现有代码和实测证据，不以猜测补齐。

## 1. 项目目标

`qq-chat-local-reader` 是一个可迁移、可公开托管到 GitHub 的 Windows 本地只读 QQ NT 聊天读取工具。

核心能力独立于 MCP：用户可以通过 Windows 图形界面或命令行同步、浏览、搜索和导出本人账号的本地聊天记录；MCP 只是提供给 Codex 及其他兼容 AI 客户端的调用入口。

第一版面向用户本人在不同 Windows 设备上重复使用，不以服务端部署或多人共享数据库为目标。

仓库公开托管，但采用 PolyForm Noncommercial License 1.0.0：允许许可证定义范围内的非商业使用、修改和分发；商业使用必须取得版权所有者另行书面授权。项目应表述为 source-available，不宣称为 OSI 意义上的开源软件。标准许可证正文不做自定义改写。

## 2. 已确认范围

### 2.1 平台与数据源

- 第一版只支持 Windows 上当前已登录的 QQ NT。
- 自动发现本机 QQ 账号、进程和数据库位置。
- 多账号同时登录时必须明确选择账号。
- 直接读取当前设备上的 QQ 本地数据库，不要求用户先手工导出 `.bak`。
- 原始 QQ 数据库始终只读；实际解析针对经过一致性校验的临时只读快照进行。
- QQ 保持登录和运行，不要求用户退出；适配器必须识别主数据库及获得一致快照所需的 WAL/SHM 等配套状态，不能只复制单个文件后假设数据完整。
- 不修改或移动旧工作区中的聊天记录和中间数据。

### 2.2 消息类型

- 支持群聊和私聊。
- 提取文本、表情占位和引用关系。
- 图片、语音、视频和文件只提取可验证的元数据，例如类型、文件名和本地路径。
- 合并转发聊天记录默认只显示本地可验证的卡片标题、条目数和摘要等元数据；只有用户明确指定 `include_forwarded=true` 时才解析本地确实存在的嵌套文字、发送者和时间。
- 合并转发中的图片、语音、视频和文件仍只提供元数据；无法完整解析时标记为“部分可用”，不得补写缺失内容。脱敏和不可信数据规则同样适用于嵌套消息。
- 第一版不做 OCR、语音转写、视频理解或无法验证的媒体内容补写。

### 2.3 使用入口

同一个发布程序提供三类入口：

```text
qq-chat-local-reader.exe                  # 默认启动 Windows 图形界面
qq-chat-local-reader.exe <command>        # 命令行操作
qq-chat-local-reader.exe mcp              # STDIO MCP 服务
```

- GitHub Release 提供无需预装 Python 或其他运行时的 Windows 安装版和便携版。
- 安装版由一个安装程序部署，安装后使用一个主程序入口；内部依赖放在受控程序目录。
- 便携版以 ZIP 发布，解压后运行同一个主 `.exe`，无需安装；GUI、CLI 和 MCP 均由该主程序提供。
- 真正的物理单文件便携版不作为第一版硬指标，只有在启动速度、临时解包、提权助手和 Defender 验证通过后才额外发布。
- 第一版允许发布未受 Windows 公共信任签名的测试构建，并透明说明 SmartScreen 及安全软件可能提示；不得要求用户关闭 Defender 或安装自签根证书。
- 发布流程生成可验证的仓库构建来源证明，但不得把它宣传为 Windows Authenticode 签名。
- 源码采用可维护、可测试的标准项目结构。
- 安装器把程序放到稳定的用户目录。
- 安装器提供默认勾选的“注册到 Codex”选项，但只有用户确认后才修改 Codex MCP 配置。
- 注册 Codex 时优先使用官方 MCP 添加/删除方式；确实不可用时才结构化修改配置文件，修改前创建可恢复备份并采用原子写入，失败时恢复原文件。
- 安装器只管理自己创建且仍指向本程序的 `qq-chat-local-reader` 注册项，不修改其他 MCP 或用户设置；卸载时发现该项已被用户改作其他用途则保留并提示。
- Codex 配置中的程序路径、工作目录和参数分别存储，正确处理空格与非 ASCII 路径，不依赖易受 PowerShell 提前解析影响的嵌套引号命令。
- 便携版不自动修改系统或 Codex 配置。
- 第一版只自动注册 Codex。
- 图形界面提供“复制通用 MCP 配置”，供其他兼容客户端手动配置。
- 默认只检查 GitHub Release 发布元数据并提示新版本，不自动下载或安装；用户可在设置中完全关闭检查。
- 更新检查不得携带聊天内容、QQ 账号、数据库路径或设备标识。
- 核心读取、索引、搜索和导出始终可完全离线运行；默认联网仅访问项目固定的 GitHub Release API 查询版本元数据。
- 不生成或发送唯一安装 ID；关闭更新检查后程序不得再主动联网。
- GitHub 返回的版本说明、链接和文件名一律按不可信输入处理；程序不执行远端命令，也不自动下载或安装。
- 未来增加任何其他联网能力前必须重新取得明确授权并更新隐私说明，不能借更新检查扩展数据传输范围。
- 第一版不包含使用遥测或自动崩溃上传；诊断报告只在本机脱敏生成，并且仅由用户主动复制或提交。
- 卸载时显示“同时删除本地聊天索引和信任配置”，默认勾选删除；用户可主动取消以便重装后复用。
- Codex MCP 注册项随程序卸载自动移除，避免保留失效入口。
- 预留 MSIX 打包能力；核心功能稳定后再尝试 Microsoft Store 免费签名和发布。
- Microsoft Store 上架及 `allowElevation` 受限能力审批不属于第一版验收条件，不得阻塞第一版交付。

### 2.4 图形界面

- 第一版界面仅提供简体中文，但所有用户可见文本集中管理并预留国际化结构；README 提供中英文入口。
- 账号选择。
- 群聊/私聊选择，允许显式全选，但不允许隐式全选。
- 日期范围选择。
- 同步状态与可理解的错误提示。
- 基础消息浏览。
- 按关键词、发言人和日期筛选。
- 展开消息上下文。
- 查看附件元数据。
- 导出格式使用互斥下拉选项：Markdown、JSON、CSV，默认 Markdown。
- 导出提供“原始 / 基础脱敏”开关，默认原始输出。
- 基础脱敏对消息正文、昵称、群名片、文件名、引用摘要等所有可见字段隐藏 QQ 号、手机号、身份证号和本地附件绝对路径；昵称或群名片中的敏感片段同样必须遮盖。
- 同一原始身份在单份导出内使用稳定匿名标识以维持对话关系，不同导出默认使用不同映射以降低跨文件关联风险。
- CSV 中可能被表格软件解释为公式的单元格必须按安全文本编码；导出文件名不得直接使用未经处理的群名、昵称或消息内容，必须移除路径分隔符和控制字符。
- 第一版不实现仿 QQ 的气泡聊天界面。

### 2.5 默认同步范围

- 用户必须明确选择至少一个群聊或私聊；未选择会话时拒绝开始并提示选择。
- 用户未指定时间范围时，默认包含今天在内的最近 7 个自然日，即从第 7 天的 00:00 到运行时刻。
- 索引内部保存 UTC 时间戳并保留消息来源中的原始时间值；GUI、CLI 和 MCP 默认按当前 Windows 时区解释并显示日期。
- 所有内部查询范围统一使用半开区间 `[开始时间, 结束时间)`；“最近 7 个自然日”精确定义为当前本地日期向前 6 天的 00:00 到当前运行时刻。
- 用户选择完整结束日期时，界面语义包含该日，内部转换为结束日期次日 00:00 的排他上界。
- 不默认同步全部历史消息。
- GUI 和 CLI 不设置历史同步硬上限；预计范围超过 90 天或消息量较大时，先显示时间与空间预估并再次确认。
- MCP 单次同步批次默认最多覆盖 31 天；更大范围自动拆分为可恢复批次并报告进度。
- MCP 分批使用首尾相接的半开时间区间，避免批次边界重复或遗漏。
- 同一账号、会话和 QQ 稳定消息标识构成幂等写入依据；重复同步不得生成重复记录。
- 如果某类消息没有可靠的单一 ID，适配器必须使用经过验证且标明来源的复合标识，不得只按正文和时间猜测去重。
- 同步按完整批次提交；失败批次不保留半成品，之前已完成批次保留并可断点继续。
- 再次观察到编辑、撤回或本地状态变化时，只更新当前可验证的观察状态，不推断服务端历史。
- 不因单次快照暂时看不到旧消息就自动从索引删除；清理需要明确证据或用户主动操作。

### 2.6 本地索引与隐私

- 解析后的规范化消息索引允许长期保存在本机，以加快后续搜索和导出。
- 索引数据库使用独立密钥加密。
- 索引密钥由 Windows 当前用户账户保护。
- 索引文件和包装后的索引密钥限制为当前 Windows 用户访问；数据库事务日志、临时排序文件和崩溃转储不得留下明文聊天内容。
- Windows 用户保护主要防止其他本机账户或离线复制磁盘后直接读取，不能抵御已经控制当前用户、管理员权限或读取器进程的恶意软件。
- 原始 QQ 数据库仅生成到临时目录的一致性只读快照，任务结束后删除；快照必须通过数据库完整性、账号归属、必要表结构和所选时间边界校验。
- Windows 上优先使用短生命周期 VSS 卷影副本取得同一时刻的主库、WAL 及版本适配器声明的配套文件。QQ 未提供 VSS Writer 时按崩溃一致状态处理，必须由 SQLite/SQLCipher 恢复与完整性校验确认可用，不能把“复制成功”视为“数据库一致”。
- VSS 创建或读取失败时不得退化为边写边逐文件复制；应停止同步并提示用户重试。卷影副本复制结束后立即删除，不能作为长期备份留在系统中。
- 提供“一键清除索引”。
- 一键清除和卸载只承诺删除应用管理的文件及密钥引用；受 SSD、备份和文件系统机制影响，不承诺物理介质上的数据绝对不可恢复。
- QQ 原始数据库密钥不长期保存。
- 密钥、聊天正文和数据库路径不得进入日志。
- 进程内存检查只申请 `PROCESS_QUERY_INFORMATION | PROCESS_VM_READ`，不得申请写入、内存操作、暂停、终止或全访问权限，也不得为扫描启用调试特权。
- 密钥候选以可清零的字节缓冲区传递，不转换为托管字符串；每个候选只交给本地 SQLCipher 结构校验，扫描缓冲区在完成、取消或异常时清零。
- 用户主动导出的 Markdown、JSON、CSV 是普通明文文件，不再受索引加密保护；导出前必须明确提示。
- 同步时如确有需要，才请求 Windows 管理员权限；浏览、搜索、导出和 MCP 查询不提权。
- 主程序和 MCP 服务始终保持普通权限；只有经实际验证确有必要的同步助手操作才按需提权，不为形式上的“安全架构”增加无效进程或权限层级。
- QQ 原始数据库密钥不得通过命令行参数、环境变量、标准输入输出、剪贴板或临时文件传递。
- 如需跨权限边界协作，只使用限制为当前 Windows 用户访问的本地安全通信通道；优先让提权助手完成一致性只读快照及必要的受限解密，尽量不将原始密钥交给主程序。
- 禁止向 QQ 进程注入代码、修改内存或挂钩函数；只允许实现读取目的所需的最小只读访问。
- 操作完成后立即释放敏感缓冲区；文档不得承诺托管运行时无法保证的绝对内存清零。
- 最终实现以实测所需的最小权限和最少组件为准：若某一步无需提权或无需跨进程，必须删除相应复杂度。

### 2.7 QQ 版本兼容策略

- 不猜测数据库或接口用法；实现前查阅已有代码和文档。
- 对未经验证的新 QQ 版本执行只读副本“安全试读”。
- 只有数据库结构、解密与解析校验全部通过才继续同步。
- 任一校验失败立即停止进一步解析，不修改原数据库、不提交索引结果，并显示 QQ 版本和不含敏感信息的诊断结果。
- 如果当前 QQ 版本无法在运行中获得一致快照，则明确失败，不返回可能缺失消息或混入其他账号的数据。
- 适配层应可替换，避免把进程密钥获取、数据库解密和消息解析耦合在一起。

## 3. MCP 行为

### 3.1 定位

MCP 读取同一套加密规范化索引，不实现一套独立的数据逻辑。检索与总结分离：读取器返回结构化、可引用的原始消息，AI 负责主题判断、归纳与报告生成。

### 3.2 AI 发起同步

- 允许 AI 通过 MCP 直接发起指定账号、群聊/联系人和时间范围的同步。
- 同步调用必须使用明确会话标识；不接受可能命中多个对象的模糊目标。
- 未指定日期时使用最近 7 个自然日默认值。
- 默认显示一次包含实际同步范围的本地确认。
- 对支持工具审批的 Codex，按会写入本地索引的工具配置审批。
- 对没有 MCP 审批能力的客户端，由读取器显示 Windows 本地确认窗口，不直接拒绝。
- 图形界面提供“信任此本机 MCP 注册配置”开关；信任后可以免除逐次确认。
- 每个 Codex 或其他 MCP 注册入口使用独立的随机授权配置，并由 Windows 当前用户账户保护；用户可以分别信任或撤销。
- 不使用客户端自行声明的名称作为身份或授权依据；复制通用 MCP 配置时生成新的独立授权配置，不继承 Codex 的信任状态。
- 授权秘密不得出现在命令行参数、环境变量、公开配置或日志中。
- 即使注册配置受信任，也不允许隐式全选或无边界同步。
- `sync_messages` 在完成范围确认后创建后台同步作业并立即返回 `job_id`，避免单次 MCP 调用因全量同步而超时；后续状态查询不重复请求授权。
- 提供同步状态查询和取消；程序或 MCP 重启后可识别完整成功的批次并安全续传。
- 本地确认窗口无法显示或 120 秒内无人操作时，本次同步安全结束并返回可操作提示，不无限挂起。

### 3.3 初步工具集合

以下名称仍可在实现前按 MCP 规范微调，但职责已经确定：

- 列出本机可用 QQ 账号。
- 列出指定账号的群聊和私聊。
- 同步明确会话与时间范围。
- 搜索已索引消息。
- 查询后台同步作业状态并按需取消。
- 获取某条消息前后的上下文。
- 获取群成员/联系人可验证的展示信息。
- 将查询结果导出为 Markdown、JSON 或 CSV。
- 查看索引状态与覆盖的时间范围。

所有查询结果需包含适用字段：账号、会话类型、会话标识、会话名称、消息 ID、时间、发送者标识、群名片/昵称、原文、消息类型、引用关系和附件元数据。

- 搜索默认每页 100 条、单页最多 500 条，使用稳定游标继续获取。
- 上下文读取默认前后各 20 条，并设置由实现验证确定的合理最大值。
- 大型导出写入用户选择或应用管理的导出目录；MCP 返回文件路径、格式、消息数量和时间范围，不在单次响应中返回整份聊天。

## 4. 安全边界

- 仅处理用户本人当前 Windows 账户可访问的本机 QQ 数据。
- 除已披露且可关闭的 GitHub Release 元数据检查外，不上传聊天记录、数据库、索引、密钥或设备识别信息。
- 不修改 QQ 数据库。
- 聊天正文一律视为不可信原始数据；结构化返回和 MCP 说明必须明确标记其数据属性，读取器不得将其当成操作指令。程序只能降低提示注入风险，不能承诺下游 AI 一定不受聊天内容影响。
- 导出和 MCP 返回不得凭空推断图片、语音、视频或转发内容。
- 会话名称重名时必须使用稳定标识消歧。
- 日志只记录非敏感的阶段、版本、计数和错误码。
- Git 仓库不得纳入真实聊天、数据库、密钥、缓存、临时副本或包含这些内容的测试产物。
- 第三方依赖必须逐项满足再分发条件，并避免与项目的非商业发布方式冲突。
- 在接受外部代码贡献前另行确定贡献者协议，避免贡献版权阻碍后续商业授权。

## 5. 架构原则

```text
QQ NT 发现与版本识别
          ↓
进程密钥获取适配器（按需提权、密钥仅驻留内存）
          ↓
一致性只读数据库快照与解密适配器
          ↓
消息/联系人/群信息解析与规范化
          ↓
Windows 用户保护的加密本地索引
          ↓
统一应用服务层
      ↙       ↓       ↘
    GUI      CLI      MCP
```

- GUI、CLI 和 MCP 复用同一应用服务层。
- 优先复用可靠的现有协议实现和组件，不重复实现成熟能力。
- 版本相关代码集中在适配层。
- 导出器只消费规范化消息模型，不直接读取 QQ 数据库。

## 6. 验收标准

第一版至少应能在受支持的 Windows QQ NT 环境中完成以下闭环：

1. 识别一个或多个已登录账号。
2. 列出群聊和私聊并正确处理重名。
3. 明确选择会话，未选择日期时同步最近 7 个自然日。
4. 不修改原库，将一致性只读快照解析到加密索引，结束后删除临时快照且不保存 QQ 原始密钥。
5. 正确显示文本、表情占位、引用关系和附件元数据。
6. 能按关键词、发言人和日期搜索，并读取前后上下文。
7. 能导出有效的 Markdown、JSON、CSV。
8. GUI、CLI、MCP 对同一查询返回一致的数据范围、消息稳定标识集合和排序；重复同步后记录数不得无故增加。
9. AI 发起同步时执行已确认的本地审批/可信注册配置策略。
10. 未验证 QQ 版本只有在安全试读全部通过后才读取，否则以非敏感诊断安全失败。
11. 发布的安装版和便携版均无需用户预装 Python 或其他运行时；安装器可经确认注册 Codex MCP。
12. Markdown、JSON、CSV 导出的消息数量和稳定标识一致。
13. 基础脱敏输出不残留原 QQ 号、手机号、身份证号或本地附件绝对路径。
14. 临时快照、明文临时文件和敏感通信资源在正常结束、取消和可控错误后均被清理。
15. 未支持 QQ 版本必须明确失败，不得返回部分索引并标记为成功。
16. 真实验收的自动输出只包含数量、类型、时间边界和错误码；聊天正文只在 GUI 中由用户人工抽查，不打印到测试输出。
17. Defender/SmartScreen 验收记录实际结果，但未签名测试构建出现信誉警告本身不判定为功能失败。
18. 只执行与上述功能和风险直接相关的必要测试。

真实环境最终验收使用用户在不受 Git 跟踪的 `.local/acceptance.toml` 中明确配置的 `<ACCEPTANCE_GROUP_ID>` 和 `<ACCEPTANCE_PRIVATE_ID>`。这些标识只属于本机验收配置，不得成为产品默认值或进入公开设计文档。绝大多数测试使用不含真实个人数据的合成夹具；真实验收数据只保留在本机受保护索引中，不进入 Git、测试日志或固定测试资源，也不从旧工作区复制。

两类真实验收均先限定为包含当天在内的最近 7 个自然日；如果目标会话没有消息，停止并请用户另行指定日期，不自动扩大到更早记录。

## 7. 实施前技术决策

以下事项已授权由实施者在查阅文档、研究现有代码和完成最小实测后决定，并在代码落地前记录最终选择与取舍：

- 应用技术栈与 GUI 框架。
- 规范化消息模型的精确字段及不同 QQ 消息类型的映射。
- 第一版需支持的具体 QQ NT 版本基线。
- 本地索引加密实现及 Windows 账户保护的具体方式。
- 安装器的具体实现方案。

## 8. 变更记录

- 2026-08-30：根据前序需求讨论及当前评审建立并完善首版设计基线。
